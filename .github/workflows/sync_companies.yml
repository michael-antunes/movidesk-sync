name: Sync Movidesk -> Companies
on:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * *"
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"name: Sync Companies
on:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * *"
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - run: |
          if [ -n "${{ vars.MOVIDESK_TOKEN }}" ]; then echo "${{ vars.MOVIDESK_TOKEN }}" > movidesk.token; fi
          if [ -n "${{ vars.NEON_DATABASE_URL }}" ]; then echo "${{ vars.NEON_DATABASE_URL }}" > neon.url; fi
          python sync_companies.py
      - uses: actions/upload-artifact@v4
        with:
          name: empresas_movidesk
          path: empresas_movidesk.csv

      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - run: |
          if [ -n "${{ vars.MOVIDESK_TOKEN }}" ]; then echo "${{ vars.MOVIDESK_TOKEN }}" > movidesk.token; fi
          if [ -n "${{ vars.NEON_DATABASE_URL }}" ]; then echo "${{ vars.NEON_DATABASE_URL }}" > neon.url; fi
          python sync_companies.py
      - uses: actions/upload-artifact@v4
        with:
          name: empresas_movidesk
          path: empresas_movidesk.csv
