name: Força resolvidos (detail_watchdog)

on:
  schedule:
    - cron: "*/20 * * * *"
  workflow_dispatch:

jobs:
  call-sync-resolved-detail:
    uses: ./.github/workflows/sync_resolved_detail.yml
    secrets: inherit
